services:
  - type: web
    name: liquidation-shield-backend
    runtime: node
    plan: free
    buildCommand: npx pnpm@11.24.0 install
    startCommand: npx pnpm@11.24.0 server
    envVars:
      - key: TICK_INTERVAL_MS
        value: "15000"
      - key: ALLOWED_ORIGIN
        value: "*"
