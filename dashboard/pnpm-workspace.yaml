# Marks this directory as its own pnpm workspace root. Without this,
# `pnpm install` here walks up to ../pnpm-workspace.yaml and installs the
# root project's dependencies instead of this app's (next, react, ...).
packages:
  - .
