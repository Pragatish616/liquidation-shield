MAINNET_RPC=https://eth-mainnet.g.alchemy.com/v2/YOUR_KEY
MAINNET_RPC_FALLBACK=
FORK_BLOCK=20500000
CHAIN=mainnet
ANVIL_PORT=8545

# --- keeper backend server (pnpm server) ---
PORT=8080
TICK_INTERVAL_MS=15000
LOG_PATH=decision-real.log.json
ALLOWED_ORIGIN=*
# optional: enables real-chain mode instead of the simulated market, only
# takes effect if an Anvil fork is also reachable at 127.0.0.1:$ANVIL_PORT
WATCH_ADDRESS=

# --- dashboard (Next.js) ---
# point the deployed dashboard at a deployed backend instead of local files
BACKEND_URL=
